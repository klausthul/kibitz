{
    IBClasses = (
        {
            CLASS = ChessView; 
            LANGUAGE = ObjC; 
            OUTLETS = {appController = AppController; game = Game; }; 
            SUPERCLASS = NSView; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {selectedPromotionPiece = id; toggleSeekDrawer = id; }; 
            CLASS = GameWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                chessServerListControl = ChessServerListControl; 
                chessView = ChessView; 
                lowerClock = NSTextField; 
                mainWindow = NSWindow; 
                promotionPiece = NSWindow; 
                seekDrawer = NSDrawer; 
                serverInput = NSTextField; 
                serverOutput = NSTextView; 
                tableView = NSTableView; 
                upperClock = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}